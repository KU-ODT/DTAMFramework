# TrafficSim plug-in runtime dependencies.
#
# This profile is intentionally separate from the core requirements because it
# pulls in GUI/GIS packages (PyQt, rasterio, matplotlib).  For a historical
# fully pinned set, see requirements-lock.txt.
-c ../../constraints.txt
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
httpx>=0.28,<1

numpy>=1.26,<2.0
rasterio>=1.3,<2.0
matplotlib>=3.8,<4.0
Pillow>=10,<13

PyQt5>=5.15,<6
PyQtWebEngine>=5.15,<6
