"""User-side payload workspace.

Protocol validation, send/receive code, ICD documents, and SDK documentation
belong to ``dtam_client``. This package only keeps payload generator examples
that a module developer can copy or adapt.
"""
