# Ref/DTAM/DTAMOperationsConsole (Git 제외)

옛 DTAMOperationsConsole 스냅샷. 약 **577 MB** (대부분 mbtiles 등 binary).
현재 활성 모듈은 루트의 `OperationModule/` 이라 이 reference 는 git 미포함.

> `.gitignore` 가 `Ref/DTAM/DTAMOperationsConsole/**` 를 무시.
